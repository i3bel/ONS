详情页的修改意见：

编辑按钮的位置错误，应该在菜谱详情页面。
在菜谱详情页面的右上角，有一个三个点按键。里面是edit recipe和delete recipe

ingredients板块
现在在编辑scale再次取消的时候会重置为1倍。这是不对的。
右边的scale在调整完倍率之后。再次点击按钮。应该是ingredients板块里面的食材数量都变为更新之后的数量，同时上面圆圈里的人数也同步变化。
更改scale调整倍率的按钮。变为一个可以滑动的条。类似于视频下面的时间条一样的东西可以滑动。然后右侧展示当前滑动到的倍率X份。

再说ingredients板块板块里面的各个食材
每个食材之间不要加分割线
你现在的量词识别不正确。
我希望你增加量词识别的词库，我给你举例：一勺半，适量，少许，一头，一把，一只，适量，两滴。这些都是量词。

在说说method板块
里面的100度，温度需要高亮。我会输入 100度 300度 之类的而不是100C

最后是菜谱编辑界面，
里面的顺序调节按钮不起作用。这两个页面，ingredients板块和method板块的每一项按住右侧的按钮可以拖动调节各个食材或者制作步骤的调换顺序。

